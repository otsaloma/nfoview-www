// -*- coding: utf-8-unix -*-

$(document).on("ready", function() {
    // TODO: When we have a Windows installer.
    // if (platform.os.family.indexOf("Windows") === 0) {
    //     $(".download.source").hide();
    //     $(".download.windows").show();
    // }
});
